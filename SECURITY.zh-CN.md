# 安全策略

[English](./SECURITY.md) · **中文**

auth-proxy 是一个认证/鉴权中间层 —— 安全是核心特性,而非附加项。我们严肃对待安全漏洞,并感谢负责任的披露。

## 报告漏洞

**请不要为安全漏洞提交公开的 GitHub Issue。**

请使用以下**任一**渠道私下报告:

1. **GitHub 安全公告(首选):** 使用仓库的
   ["报告漏洞"](https://github.com/renxqoo/auth-proxy/security/advisories/new)
   功能。这允许我们在公开披露前私下讨论并修复问题。
2. **邮件:** 把详情发送到 GitHub 个人资料上列出的维护者邮箱,邮件标题以 `[SECURITY] auth-proxy` 开头。

请在报告中包含:

- 漏洞描述及其潜在影响。
- 你测试的具体版本 / commit。
- 复现步骤(PoC、日志或最小测试用例)。
- 如有,建议的缓解措施。

## 响应时间

| 环节 | 目标 |
|------|------|
| 确认收到报告 | **48 小时**内 |
| 初步评估 + 严重程度评级 | **5 个工作日**内 |
| 高危漏洞的修复或缓解 | 确认后 **30 天**内 |
| 协调公开披露 | 修复发布后(除非你要求匿名,否则署名致谢) |

整个过程中我们会持续同步进展。若超过上述时限仍未收到回复,请跟进催促。

## 范围

在范围内:

- 中间层(`apps/server`)和管理后台(`apps/admin-web`)的运行时行为。
- 认证流程(device flow、token 签发、refresh 轮换、重用检测)。
- Token 处理(JWT 签名/验签、company token 代理)。
- 限流、CSRF、session 管理、管理员认证。

不在范围内:

- 依赖项中的漏洞 —— 请向上游报告(本项目依赖 Dependabot + 定期更新)。
- `company-mock` 应用中的问题 —— 它只是本地开发的 **mock**,绝不部署到生产。
- 自行配置不当导致的问题(如提交了真实 `.env`、手动暴露端口等)。

## 支持的版本

仅最新发布线(`main`)接收安全更新,目前没有 LTS 分支。

| 版本 | 支持 |
|------|------|
| 最新(`main`) | ✅ |
| 更早版本 | ❌ |

## 披露策略

- 我们遵循**协调披露**:细节在修复可用后才公开。
- 除非你要求匿名,否则会在公告中署名致谢。
- 恳请在修复发布前不要公开披露漏洞。

## 加固说明

生产加固细节 —— 强制的 secret 长度、`assertProductionConfig()`、host header 注入防护、refresh 重用检测 —— 详见 [docs/docker/04-config-secrets.md](./docs/docker/04-config-secrets.md)。部署前请务必阅读。

感谢你帮助保护 auth-proxy 及其用户的安全。
